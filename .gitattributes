# Normalize line endings. Shell scripts and the macOS .command launcher MUST be
# LF or they break on macOS/Linux; PowerShell stays CRLF; images are binary.
* text=auto

*.sh        text eol=lf
*.command   text eol=lf
*.ps1       text eol=crlf

*.png binary
*.ico binary
*.icns binary
